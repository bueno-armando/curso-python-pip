GIT
make a new commit:
git add *
git commit -m "commit name"
git push origin master

add ignored files to commit
git add .

status 
git status

pip3 freeeze => arbol de librerias
pwd => current directory
mkdir => make directory
ll => list sub folders
cd => change directory
clear => clear terminal
git init => initalize project in git
touch => create file

git remote -v => current repo

dependencies

build-eseentials
libssl-dev
libffi-dev

which python3 => ubicacion de python3
which pip3 => ubicacion de pip3
python3 -m venv [nombre ambiente] => crear ambiente
source [nombre]/bin/activate => activar ambiente
deactivate => salie del amb virtual
Las dependencias en un ambiente virtual están AISLADAS